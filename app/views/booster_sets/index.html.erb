<div class="title inner-border">
  <h1>Booster Sets</h1>
</div>

<div class="container">
  <div class="text inner-border style-border">
    Each main series Booster Pack is guaranteed a Rare, 7 Commons,
    and a ninth card which will either be another Common or a foil
    card (Super Rare rarity or higher).
  </div>
</div>

<div class="container">
  <div class="booster-sets inner-border style-border">
    <% @booster_sets.each do |booster| %>
      <%= link_to booster_set_booster_packs_path(booster) do %>
        <div class="booster-set">
          <img src="<%= booster.picture %>" alt="Booster Set Image">
        </div>
      <% end %>
    <% end %>
  </div>
</div>
