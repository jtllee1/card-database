<div class="title inner-border"><h1>Your Pack</h1></div>

<div class="container">
  <div class="text inner-border style-border">
    Each main series Booster Pack is guaranteed a Rare, 7 Commons,
    and a ninth card which will either be another Common or a foil
    card (Super Rare rarity or higher).
  </div>
</div>

<div class="container">
  <a href="#pack" class="button" data-turbolinks-action="replace">Open another pack</a>
</div>

<div class="container">
  <div class="pack-cards inner-border style-border" id="pack">
    <% @pack.each do |card| %>
      <div class="pack-card">
        <%= link_to card_path(card) do %>
          <img src="<%= card.card.picture %>" alt="Card Image" class="<%= card.card_rarity %>">
        <% end %>
      </div>
    <% end %>
  </div>
</div>
